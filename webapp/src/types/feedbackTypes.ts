export interface FeedbackData {
    title: string;
    description: string;
    userId: string;
}

export interface Feedback {
    id: string;
    title: string;
    description: string;
    userId: string;
    createdAt: string;
    updatedAt: string;
}